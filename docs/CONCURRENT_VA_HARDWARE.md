# Guarded real-VA concurrent campaign (not executed here)

**AI disclosure:** Generated by AI. Hardware execution and claims stay in
[libva-v4l2_request#36](https://github.com/iconidentify/libva-v4l2_request/issues/36).
This leaf does not run the campaign or grant a decoder lease.

## Identities

- Driver: `avd-fixes` starting `c8e4aecf757c465a583f2b3534f1e617277a4721`
- Helper: `tests/concurrent-va-worker`
- Corpus: generated `ffmpeg` lavfi `testsrc` H.264 64×64 clips (recipe in `tests/concurrent-va-run.py`)
- Software oracle: per-worker `frames=` / `MD5=` from `software` mode; hardware must match counts and must use `AV_PIX_FMT_VAAPI`

## Schedules (ten repetitions each)

1. 1/2/4 threads, one shared VA display
2. 1/2/4 processes, one worker each
3. Mid-run teardown of stream 0 while others continue (kept frame)
4. Client-level invalid API after teardown (`avcodec_send_packet` on a freed context)

Stop on first new kernel fault, wedge, foreign `/dev/video0` holder or software fallback. Preserve partial logs. Exclusive `hwguard.py` lease. Restore idle decoder.

Offline tests never set `LIBVA_DRIVERS_PATH` to relabel the model `concurrent-stress` worker as this client.
